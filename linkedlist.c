#include <stdio.h>
#include <stdlib.h>
// #include "linkedlist.h"

// Student_t *add_to_last(Student_t *head, char name, Grade_t *grade)
// {
//     Student_t *pos = head;

//     if (!pos)
//     {
//         pos = (Student_t *)malloc(sizeof(Student_t));
//         pos->name = name;
//         pos->grade = grade;
//         pos->next = NULL;
//         return pos;
//     }
//     else
//     {
//         while (pos->next)
//         {
//             pos = pos->next;
//         }
//         pos->next = (Student_t *)malloc(sizeof(Student_t));
//         pos->next->name = name;
//         pos->next->grade = grade;
//         pos->next->next = NULL;
//     }
//     return head;
// }

// Grade_t* add_to_last(Grade_t* head, id id,int grade)
// {
//     Grade_t *pos = head;

//     if (!pos)
//     {
//         pos = (Student_t *)malloc(sizeof(Student_t));
//         pos->id = id;
//         pos->grade = grade;
//         pos->next = NULL;
//         return pos;
//     }
//     else
//     {
//         while (pos->next)
//         {
//             pos = pos->next;
//         }
//         pos->next = (Student_t *)malloc(sizeof(Student_t));
//         pos->next->id = id;
//         pos->next->grade = grade;
//         pos->next->next = NULL;
//     }
//     return head;
// }



